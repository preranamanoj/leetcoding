class Solution:
    def twoSum(self, nums, target):
        j = len (nums) # that would be 4 in this case 
        for i in range(j): # 0-3
            if nums[i] + nums[i+1] == target:
                return [i,i+1]
